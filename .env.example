MONGO_URL=mongodb://localhost:27017
DB_NAME=pubg_uc_store
NEXT_PUBLIC_BASE_URL=https://your-domain.com
CORS_ORIGINS=*

# JWT Secret for admin authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Master Encryption Key for sensitive data (Shopier credentials)
# CRITICAL: Keep this secret! Generate a strong random key
# Example: openssl rand -base64 32
MASTER_ENCRYPTION_KEY=your-master-encryption-key-change-this-to-random-32-byte-key